import setuptools

# Setup configuration is in setup.cfg
# You can enable the invocation of the building block by
# defining and uncomenting the:
#    Options -> Scripts -> binary name
# in the setup.cfg file.

setuptools.setup()
